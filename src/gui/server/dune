(library
  (name server)
  (flags -w)
  (preprocess
    (pps ppx_deriving_yojson lwt_ppx))
  (libraries
	  opium bacterie_libs reactors lwt_ppx websocket-lwt-unix lwt-pipe
    yaac_db
    caqti-lwt
))
